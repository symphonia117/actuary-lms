window.appData.chapters = window.appData.chapters.concat([
    {
        "id": "ch1",
        "title": "Chapter 1: General Probability",
        "content": "<h3>1.1 Sets and Events</h3><p>Probability theory begins with the concept of a <strong>sample space</strong>, denoted by $S$ or $\\Omega$, representing the set of all possible outcomes of a random experiment. An <strong>event</strong> is any subset of the sample space.</p><p>Key set operations include:</p><ul><li><strong>Union ($A \\cup B$):</strong> The event that either $A$ or $B$ (or both) occurs.</li><li><strong>Intersection ($A \\cap B$):</strong> The event that both $A$ and $B$ occur.</li><li><strong>Complement ($A'$ or $A^c$):</strong> The event that $A$ does not occur.</li></ul><p>Two events are <strong>mutually exclusive</strong> (or disjoint) if they cannot occur simultaneously, meaning $A \\cap B = \\emptyset$.</p><h3>1.2 Axioms of Probability</h3><p>Kolmogorov's axioms are the foundation of probability:</p><ol><li>$0 \\leq P(A) \\leq 1$ for any event $A$.</li><li>$P(S) = 1$.</li><li>If $A_1, A_2, \\dots$ are mutually exclusive events, then $P(\\bigcup_{i=1}^\\infty A_i) = \\sum_{i=1}^\\infty P(A_i)$.</li></ol><p>From these, we derive the Addition Rule: $$P(A \\cup B) = P(A) + P(B) - P(A \\cap B)$$</p><h3>1.3 Conditional Probability</h3><p>The probability of event $A$ occurring given that event $B$ has already occurred is $P(A|B)$, calculated as:</p><p>$$P(A|B) = \\frac{P(A \\cap B)}{P(B)}, \\quad \\text{where } P(B) > 0$$</p><h3>1.4 Independence</h3><p>Two events $A$ and $B$ are <strong>independent</strong> if the occurrence of one does not affect the probability of the other:</p><p>$$P(A \\cap B) = P(A)P(B)$$</p><h3>1.5 Law of Total Probability and Bayes' Theorem</h3><p>If $B_1, B_2, \\dots, B_n$ partition the sample space, the Law of Total Probability states:</p><p>$$P(A) = \\sum_{i=1}^n P(A|B_i)P(B_i)$$</p><p>Bayes' Theorem updates probabilities based on new evidence:</p><p>$$P(B_k|A) = \\frac{P(A|B_k)P(B_k)}{\\sum_{i=1}^n P(A|B_i)P(B_i)}$$</p>"
    },
    {
        "id": "ch2",
        "title": "Chapter 2: Univariate Random Variables",
        "content": "<h3>2.1 Definition and Classification</h3><p>A <strong>random variable</strong> $X$ assigns a real number to each outcome in a sample space. They are broadly classified as:</p><ul><li><strong>Discrete:</strong> Takes on a countable number of values. Described by a Probability Mass Function (PMF), $p(x) = P(X = x)$.</li><li><strong>Continuous:</strong> Takes on an uncountable infinite number of values. Described by a Probability Density Function (PDF), $f(x)$.</li></ul><h3>2.2 Cumulative Distribution Function (CDF)</h3><p>The CDF, $F(x)$, gives the probability that $X$ is less than or equal to $x$:</p><p>$$F(x) = P(X \\leq x)$$</p><p>For discrete variables, $F(x) = \\sum_{t \\leq x} p(t)$. For continuous variables, $F(x) = \\int_{-\\infty}^x f(t)dt$.</p><p>Key properties of the CDF include:</p><ul><li>$F(-\\infty) = 0$ and $F(\\infty) = 1$.</li><li>$F(x)$ is a non-decreasing function.</li><li>$P(a < X \\leq b) = F(b) - F(a)$.</li></ul><h3>2.3 Survival Function</h3><p>The survival function, $S(x)$, is the complement of the CDF, representing the probability that the random variable exceeds a certain value:</p><p>$$S(x) = P(X > x) = 1 - F(x)$$</p><p>It is extensively used in actuarial science to model time-until-death or time-until-failure.</p>"
    },
    {
        "id": "ch3",
        "title": "Chapter 3: Multivariate Random Variables",
        "content": "<h3>3.1 Joint Distributions</h3><p>The joint behavior of two random variables $X$ and $Y$ is described by their joint distribution. The joint CDF is $F(x,y) = P(X \\leq x, Y \\leq y)$.</p><p>For discrete variables, we use the joint PMF $p(x,y) = P(X=x, Y=y)$. For continuous variables, we use the joint PDF $f(x,y)$, where probabilities are found via double integration:</p><p>$$P((X,Y) \\in A) = \\iint_A f(x,y) dx dy$$</p><h3>3.2 Marginal Distributions</h3><p>The marginal distribution of $X$ considers $X$ unconditionally, effectively integrating or summing out $Y$:</p><p>$$f_X(x) = \\int_{-\\infty}^\\infty f(x,y) dy \\quad \\text{(continuous)}$$</p><p>$$p_X(x) = \\sum_y p(x,y) \\quad \\text{(discrete)}$$</p><h3>3.3 Conditional Distributions</h3><p>The conditional PDF (or PMF) of $X$ given $Y=y$ is:</p><p>$$f_{X|Y}(x|y) = \\frac{f(x,y)}{f_Y(y)}, \\quad \\text{provided } f_Y(y) > 0$$</p><h3>3.4 Independence of Random Variables</h3><p>Random variables $X$ and $Y$ are independent if and only if their joint distribution is the product of their marginal distributions:</p><p>$$f(x,y) = f_X(x)f_Y(y) \\quad \\text{for all } x, y$$</p>"
    },
    {
        "id": "ch4",
        "title": "Chapter 4: Expectation, Variance, and Moments",
        "content": "<h3>4.1 Expected Value (Mean)</h3><p>The expected value represents the probability-weighted average or \"center of mass\" of a random variable.</p><p>$$E[X] = \\sum x p(x) \\quad \\text{(discrete)}$$</p><p>$$E[X] = \\int_{-\\infty}^\\infty x f(x) dx \\quad \\text{(continuous)}$$</p><h3>4.2 Variance and Standard Deviation</h3><p>Variance measures the dispersion of the distribution around the mean $\\mu = E[X]$.</p><p>$$Var(X) = E[(X - \\mu)^2] = E[X^2] - (E[X])^2$$</p><p>The standard deviation is the square root of the variance, $\\sigma = \\sqrt{Var(X)}$.</p><h3>4.3 Covariance and Correlation</h3><p>Covariance measures the linear association between two random variables:</p><p>$$Cov(X, Y) = E[(X - \\mu_X)(Y - \\mu_Y)] = E[XY] - E[X]E[Y]$$</p><p>Correlation standardizes covariance to a $[-1, 1]$ scale:</p><p>$$\\rho_{X,Y} = \\frac{Cov(X,Y)}{\\sigma_X \\sigma_Y}$$</p><h3>4.4 Conditional Expectation and Variance</h3><p>The expected value of $X$ given $Y=y$ is computed using the conditional distribution. The Law of Iterated Expectations states:</p><p>$$E[X] = E[E[X|Y]]$$</p><p>The Law of Total Variance (Eve's Law) states:</p><p>$$Var(X) = E[Var(X|Y)] + Var(E[X|Y])$$</p>"
    },
    {
        "id": "ch5",
        "title": "Chapter 5: Discrete Probability Distributions",
        "content": "<h3>5.1 Binomial Distribution</h3><p>Models the number of successes in $n$ independent Bernoulli trials, each with success probability $p$. $X \\sim \\text{Bin}(n,p)$.</p><p>$$P(X=x) = \\binom{n}{x} p^x (1-p)^{n-x}, \\quad x = 0, 1, \\dots, n$$</p><p>$E[X] = np$, $Var(X) = np(1-p)$</p><h3>5.2 Poisson Distribution</h3><p>Models the number of events in a fixed interval, given an average rate $\\lambda$. $X \\sim \\text{Poisson}(\\lambda)$.</p><p>$$P(X=x) = \\frac{e^{-\\lambda} \\lambda^x}{x!}, \\quad x = 0, 1, \\dots$$</p><p>$E[X] = \\lambda$, $Var(X) = \\lambda$</p><h3>5.3 Geometric Distribution</h3><p>Models the number of trials up to and including the first success. $X \\sim \\text{Geo}(p)$.</p><p>$$P(X=x) = (1-p)^{x-1}p, \\quad x = 1, 2, \\dots$$</p><p>$E[X] = \\frac{1}{p}$, $Var(X) = \\frac{1-p}{p^2}$</p><h3>5.4 Negative Binomial Distribution</h3><p>Generalizes the Geometric distribution to the number of trials until the $r$-th success. $X \\sim \\text{NB}(r,p)$.</p><p>$$P(X=x) = \\binom{x-1}{r-1} p^r (1-p)^{x-r}, \\quad x = r, r+1, \\dots$$</p><p>$E[X] = \\frac{r}{p}$, $Var(X) = \\frac{r(1-p)}{p^2}$</p>"
    },
    {
        "id": "ch6",
        "title": "Chapter 6: Continuous Probability Distributions",
        "content": "<h3>6.1 Continuous Uniform Distribution</h3><p>Constant probability density over an interval $[a, b]$. $X \\sim U(a,b)$.</p><p>$$f(x) = \\frac{1}{b-a}, \\quad a \\leq x \\leq b$$</p><p>$E[X] = \\frac{a+b}{2}$, $Var(X) = \\frac{(b-a)^2}{12}$</p><h3>6.2 Exponential Distribution</h3><p>Models waiting times between Poisson events. It is the only continuous distribution with the memoryless property. $X \\sim \\text{Exp}(\\lambda)$.</p><p>$$f(x) = \\lambda e^{-\\lambda x}, \\quad x > 0$$</p><p>$E[X] = \\frac{1}{\\lambda}$, $Var(X) = \\frac{1}{\\lambda^2}$</p><h3>6.3 Normal Distribution</h3><p>The bell curve, ubiquitous in statistics. $X \\sim N(\\mu, \\sigma^2)$.</p><p>$$f(x) = \\frac{1}{\\sigma\\sqrt{2\\pi}} e^{-\\frac{1}{2}\\left(\\frac{x-\\mu}{\\sigma}\\right)^2}$$</p><p>The Standard Normal, $Z$, has $\\mu=0, \\sigma^2=1$. Any normal variable can be standardized: $Z = \\frac{X-\\mu}{\\sigma}$.</p><h3>6.4 Gamma Distribution</h3><p>Models the waiting time until the $\\alpha$-th Poisson event. It is a sum of $\\alpha$ independent Exponential variables. $X \\sim \\text{Gamma}(\\alpha, \\theta)$.</p><p>$$f(x) = \\frac{x^{\\alpha-1} e^{-x/\\theta}}{\\Gamma(\\alpha)\\theta^\\alpha}, \\quad x > 0$$</p><p>$E[X] = \\alpha\\theta$, $Var(X) = \\alpha\\theta^2$</p>"
    },
    {
        "id": "ch7",
        "title": "Chapter 7: Transformations of Random Variables",
        "content": "<h3>7.1 Moment Generating Functions (MGFs)</h3><p>The MGF of $X$ is $M_X(t) = E[e^{tX}]$. It uniquely identifies the distribution and simplifies moment calculation:</p><p>$$E[X^n] = \\left. \\frac{d^n}{dt^n} M_X(t) \\right|_{t=0}$$</p><p>For independent variables, $M_{X+Y}(t) = M_X(t)M_Y(t)$.</p><h3>7.2 Transformations (PDF Method / Jacobian)</h3><p>If $Y = g(X)$ where $g$ is monotonic and differentiable, the PDF of $Y$ is:</p><p>$$f_Y(y) = f_X(g^{-1}(y)) \\left| \\frac{d}{dy} g^{-1}(y) \\right|$$</p><p>For multivariate transformations, this generalizes using the determinant of the Jacobian matrix.</p><h3>7.3 Convolution</h3><p>The distribution of the sum of two independent random variables $Z = X + Y$ is found by convolution.</p><p>$$f_Z(z) = \\int_{-\\infty}^\\infty f_X(x) f_Y(z-x) dx$$</p><h3>7.4 Order Statistics</h3><p>For an i.i.d. sample $X_1, \\dots, X_n$, let $Y_k$ be the $k$-th smallest value. The minimum ($Y_1$) and maximum ($Y_n$) are critical in insurance:</p><p>Maximum CDF: $F_{Y_n}(y) = [F_X(y)]^n$</p><p>Minimum Survival: $S_{Y_1}(y) = [S_X(y)]^n$</p>"
    },
    {
        "id": "ch8",
        "title": "Chapter 8: Risk and Insurance Applications",
        "content": "<h3>8.1 Deductibles</h3><p>An ordinary deductible $d$ shifts the risk to the insured for losses up to $d$. The insurer pays $Y = \\max(0, X - d)$.</p><p>The expected payment per loss is:</p><p>$$E[Y] = \\int_d^\\infty (x - d) f(x) dx = \\int_d^\\infty S(x) dx$$</p><p>A franchise deductible pays the full loss $X$ if $X > d$, and $0$ otherwise.</p><h3>8.2 Policy Limits</h3><p>A policy limit $u$ caps the insurer's payment. The payment is $Y = \\min(X, u) = X \\wedge u$.</p><p>The expected payment is the limited expected value:</p><p>$$E[X \\wedge u] = \\int_0^u x f(x) dx + u P(X > u) = \\int_0^u S(x) dx$$</p><h3>8.3 Coinsurance</h3><p>Coinsurance with factor $\\alpha$ means the insurer pays a proportion $\\alpha$ of the cost. The payment is $Y = \\alpha X$. When combined with a deductible and limit:</p><p>$$Y = \\alpha (\\max(0, \\min(X, u) - d))$$</p><h3>8.4 Risk Measures</h3><p><strong>Value at Risk (VaR):</strong> The $p$-th quantile of the loss distribution, $\\pi_p$. $P(X > \\pi_p) = 1 - p$.</p><p><strong>Tail Value at Risk (TVaR):</strong> The expected loss given that the loss exceeds the VaR. It is a coherent risk measure, avoiding VaR's lack of subadditivity.</p><p>$$TVaR_p = E[X | X > \\pi_p]$$</p>"
    }
]);
